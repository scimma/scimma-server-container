FROM scimma/base:0.0
COPY scripts/runClient /root/runClient
RUN chmod ugo+rx /root/runClient
RUN mkdir -p /root/.config && ln -s /root/shared/kafkacat.conf /root/.config/kafkacat.conf
WORKDIR /usr/bin
CMD ["/bin/bash"]
